# vscode-acp-iot v0.3.1 版本开发记录

## 1. 目标

在 v0.3.0 串口-Chat-Agent 打通基础上，更新 barecode agent 配置并同步开发文档。

## 2. 主要变更

### barecode Agent 配置更新

- `package.json` 中 barecode agent 从 `node ../AI-Code-CLI/bin/cli.js` 改为全局 `barecode` 命令
- 通过 `npm link` 将 barecode CLI 注册为全局命令

### 工具契约演进

- v0.3.0 初始：extMethod 描述格式 → AI 不可用
- v0.3.0 更新：`[SERIAL_CALL:...]` 文本格式 → AI 不遵循
- v0.3.1：配合 CLI MCP 工具注册方案，工具契约保留为兜底

### 文档同步

- `roadmap/ROADMAP.md`：Phase 2 状态更新为已完成
- `version/v0.3.0.md`：补充完整的 5+1 步串口-Chat-Agent 打通记录

## 3. 变更文件

| 文件 | 操作 |
|------|------|
| `package.json` | 修改（版本 0.3.0 → 0.3.1，barecode agent 配置） |
| `.claude_dev/roadmap/ROADMAP.md` | 修改 |
| `.claude_dev/version/v0.3.0.md` | 修改 |

## 4. 验证

- webpack 编译通过
- CLI MCP 端到端测试 7/7 通过